# 阅读记录整理与摘录 PDF 工作流

## 首次运行

1. 安装 Node.js 和 Google Chrome。
2. 在本目录运行 `npm ci` 安装锁定版本的依赖。
   - 如果用户级 npm 缓存存在权限问题，改用 `npm ci --cache /private/tmp/book-note-npm-cache`，不要修改整个 `~/.npm` 的所有权。
3. 默认使用 `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`；如 Chrome 位于其他位置，通过 `CHROME_PATH` 指定可执行文件。

## 书目类型

README 使用统一时间线记录以下三类书：

- `电子书（有摘录）`：存在个人批注 Markdown，并生成同名 PDF。
- `电子书（无摘录）`：只有阅读记录，不创建 Markdown 或 PDF。
- `纸质书`：只有阅读记录，不创建空的 Markdown 或 PDF。

所有条目都展示书名、作者、阅读日期、阅读形式、个人评分、豆瓣评分、一个主标签和简介。有摘录的书额外展示摘录重点及 Markdown/PDF 链接；无摘录的书额外展示书评。

## 收到新摘录 Markdown 时

1. **清理内容**：移除所有包含 `[在书中查看]` 的行，以及文件末尾的空标题和 `_Generated at: ..._` 导出元数据。
2. **生成 PDF**：运行 `node convert_to_pdf.js`（使用 `marked` + `puppeteer-core`，通过无头 Chrome 渲染）。
   - 输出 A4 格式 PDF。
   - `displayHeaderFooter: false`，无水印、无页眉页脚。
   - 字体优先使用 Microsoft YaHei / SimSun，并提供 macOS 中文字体回退。
3. **验证 PDF**：确认同名 `.pdf` 已生成且文件大小大于 0。
4. **确定书目信息**：
   - 从 Markdown 标题和作者行提取书名、作者。
   - 以文件名前缀 `YYYY-MM-DD-HH-mm-ss` 作为阅读与排序时间，README 只显示 `YYYY-MM-DD`。
   - 阅读形式标为 `电子书（有摘录）`。
5. **询问个人评分**：向用户询问该书的个人评分，满分 10 分，允许一位小数。
   - 未取得用户评分前，不得猜测评分、根据书评推断评分或写入占位分数。
6. **准备简介和摘录重点**：
   - 简介优先参考出版社、作者主页或可靠图书页面，并用自己的语言概括，不得照抄官方简介。
   - 摘录重点只根据个人摘录内容归纳，不把摘录文件描述成完整书稿。
7. **核验豆瓣评分**：按“豆瓣评分核验规则”处理。
8. **更新 README**：按“README 索引规则”加入统一时间线。

## 收到纸质书或无摘录电子书记录时

1. **收集必要信息**：询问书名、作者、阅读形式、阅读日期和个人评分。
   - 阅读形式只能标为 `纸质书` 或 `电子书（无摘录）`。
   - 阅读日期由用户提供，使用 `YYYY-MM-DD`；信息不完整时先询问，不得自行猜测。
   - 仅当用户明确允许估算日期时，才可在用户指定的月份或时间范围内按阅读顺序均匀安排用于排序的 `YYYY-MM-DD` 日期；后续取得准确日期时应更新。
   - 个人评分满分 10 分，允许一位小数；未取得评分前不新增条目。
2. **准备简介**：优先参考出版社、作者主页或可靠图书页面，重新概括内容，不照抄来源文案。
3. **准备书评**：根据可靠的公开评论资料生成简洁、平衡的评论。
   - 书评是对作品特点和常见评价的整理，不得冒充用户的个人观点。
   - 不得根据用户评分反推褒贬，不得编造用户的阅读感受。
4. **核验豆瓣评分**：按“豆瓣评分核验规则”处理。
5. **更新 README**：加入统一时间线，只展示阅读记录，不创建空 Markdown/PDF，也不显示不存在的摘录链接。

## 豆瓣评分核验规则

1. 使用书名和作者查找豆瓣读书条目，并核对标题、作者及最接近的版本；不要使用同名书、外文版或明显不匹配的再版评分。
2. 找到可靠匹配且有评分时，写为 `[X.X/10](豆瓣条目链接)（查询于 YYYY-MM-DD）`。
3. 找到可靠匹配但尚无评分时，写为 `[暂无评分](豆瓣条目链接)（查询于 YYYY-MM-DD）`。
4. 无法可靠匹配豆瓣条目时，写为 `未找到（查询于 YYYY-MM-DD）`，不得猜测评分或链接。
5. 豆瓣评分必须与“个人评分”分开标注；查询日期使用实际查询当天日期。

## README 索引规则

1. 所有书统一按阅读时间倒序排列，不按阅读形式分组。
   - 有摘录书按文件名中的完整时间排序。
   - 无摘录书按用户提供的阅读日期排序。
   - 同一天且没有更精确时间时，新加入的条目排在前面。
   - 同一本书分多个时间段阅读时，以用户给出的卷次或范围分别记录；同一阶段的连续卷次可作为一条，但不得跨时间段合并，标题中须如实保留卷次范围。
2. 每条书目使用以下公共字段：书名、作者、阅读日期、阅读形式、个人评分、豆瓣评分、标签、简介。
   - 每本书只标注一个便于归类的主标签，优先参考豆瓣条目中的标签或图书分类，并结合书籍的核心内容选择。
   - 标签使用简洁、稳定的类别词，例如 `历史`、`投资`、`小说`、`推理`、`传记`，不得堆叠多个标签。
3. `电子书（有摘录）` 增加“摘录重点”、Markdown 链接和 PDF 链接，并明确链接内容是个人摘录，不是完整书稿。
4. `电子书（无摘录）` 和 `纸质书` 增加“书评”，不添加摘录或下载链接。
5. 简介、书评和摘录重点分别成段，避免混写；个人评分与豆瓣评分必须使用明确标签。

## 转换脚本说明

- `convert_to_pdf.js` 自动扫描脚本目录下的摘录 `.md` 文件，并排除 `README.md` 和 `AGENTS.md`。
- 每次运行都会先清理阅读链接和末尾导出元数据；仅当内容发生变化时才写回 Markdown。
- 默认只生成缺失或比 Markdown 更旧的 PDF；运行 `node convert_to_pdf.js --force` 可强制重建全部 PDF。
- PDF 与 Markdown 同名，仅扩展名不同。

## Git 交付规则

1. 每次完成文件修改并通过相应验证后，将本次改动提交到当前分支。
2. 提交完成后直接运行 `git push origin <当前分支>` 推送到远端，不需要询问用户。
3. 提交信息应简洁说明本次改动；不得提交或推送尚未完成、未验证的修改。
